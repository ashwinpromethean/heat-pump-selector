version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip3 install -r requirements.txt

run:
  runtime-version: 3.11
  command: python -m streamlit run app.py --server.port $PORT --server.address=0.0.0.0 --server.headless=true
  network:
    port: 8080
